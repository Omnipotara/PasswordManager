# Database configuration
db.url=jdbc:mysql://localhost:3306/passwordmanager_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
db.username=root
db.password=change-me

# SMTP configuration for MFA email delivery
smtp.host=smtp.gmail.com
smtp.port=587
smtp.username=change-me@example.com
smtp.password=change-me
smtp.auth=true
smtp.starttls=true
